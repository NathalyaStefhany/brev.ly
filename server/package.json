{
  "name": "server-brev.ly",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file .env src/infra/http/server.ts",
    "build": "tsup-node",
    "lint": "eslint .",
    "lint:staged": "eslint --max-warnings=0",
    "db:generate": "drizzle-kit generate",
    "db:generate:test": "dotenv -e .env.test -- drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:test": "dotenv -e .env.test -- drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "docker:test": "docker-compose -f docker-compose-tests.yml -p test down && docker-compose -f docker-compose-tests.yml -p test --env-file .env.test up --build -d",
    "test:prepare": "npm run db:generate:test && npm run db:migrate:test",
    "test": "npm run test:prepare && NODE_ENV=test dotenv -e .env.test -- vitest run",
    "test:watch": "npm run test:prepare && NODE_ENV=test dotenv -e .env.test -- vitest",
    "test:coverage": "npm run test:prepare && NODE_ENV=test dotenv -e .env.test -- vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.802.0",
    "@aws-sdk/lib-storage": "^3.802.0",
    "@fastify/cors": "^11.0.1",
    "@fastify/swagger": "^9.5.0",
    "@scalar/fastify-api-reference": "^1.28.23",
    "csv-stringify": "^6.5.2",
    "drizzle-orm": "^0.42.0",
    "fastify": "^5.3.2",
    "fastify-type-provider-zod": "^4.0.2",
    "postgres": "^3.4.5",
    "uuidv7": "^1.0.2",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@types/node": "^22.14.1",
    "@vitest/coverage-v8": "3.1.2",
    "dotenv": "^16.5.0",
    "dotenv-cli": "^8.0.0",
    "drizzle-kit": "^0.31.0",
    "eslint": "^9.25.1",
    "globals": "^16.0.0",
    "tsup": "^8.4.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.31.0",
    "vitest": "^3.1.2"
  }
}
